CREATE TABLE TB_S_RECO_PICK_EVAL (
    PICK_ID           NUMBER         NOT NULL,
    EVAL_TYPE         VARCHAR2(10)   NOT NULL,
    HOLD_DAYS         NUMBER         NOT NULL,
    ANCHOR_DATE       DATE           NOT NULL,
    TARGET_DATE       DATE,
    RETURN_PCT        NUMBER(18,4),
    MFE_PCT           NUMBER(18,4),
    MAE_PCT           NUMBER(18,4),
    WIN_YN            CHAR(1),
    TP1_HIT_YN        CHAR(1),
    STOP_HIT_YN       CHAR(1),
    CALC_STATUS       VARCHAR2(20)   DEFAULT 'PENDING' NOT NULL,
    CREATE_DATE       DATE           DEFAULT SYSDATE NOT NULL,
    MODIFY_DATE       DATE           DEFAULT SYSDATE NOT NULL,
    CONSTRAINT PK_TB_S_RECO_PICK_EVAL PRIMARY KEY (PICK_ID, EVAL_TYPE, HOLD_DAYS)
);

CREATE INDEX IX_TB_S_RECO_PICK_EVAL_01 ON TB_S_RECO_PICK_EVAL (EVAL_TYPE, HOLD_DAYS, CALC_STATUS);
CREATE INDEX IX_TB_S_RECO_PICK_EVAL_02 ON TB_S_RECO_PICK_EVAL (WIN_YN, HOLD_DAYS);
